# Vent animé (lot B2) — plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Vent à 10 m animé par particules sur globelayers.com, en surimpression combinable avec n'importe quelle couche, alimenté par deux entrées scalaires `wind_u`/`wind_v` du manifeste v2, avec interrupteur « Vent », `?wind=1|0` et tooltip « Vent 23 km/h NO » ; retrait du legacy `gfs/latest.*`.

**Architecture:** Le pipeline gagne deux `LayerSpec` (UGRD/VGRD 10 m, encodage linéaire ±60 m/s) et perd le legacy schema 1 ; le manifeste ne change pas de schéma. Côté front, une simulation **CPU pure** (`web/src/wind/sim.ts`) avance N particules à partir des pixels U/V lus par `bitmapPixels` (jamais de texture GPU), écrit un tampon slot-major `[K][N][xyz]` décalé par `copyWithin` et envoyé entier au GPU à chaque tick ; un seul `LineSegments` (`web/src/render/wind.ts`) dessine les traînées avec un alpha statique par slot. Un contrôleur cadencé à 30 Hz s'inscrit sur `SceneHandle.onFrame` seulement quand le vent est actif, ce qui conserve « 0 draw call au repos ».

**Tech Stack:** Python 3.12 sur Actions / 3.14 en local (`.venv/Scripts/python`), numpy, Pillow, requests, boto3, eccodes (bindings Python, Linux seulement) ; Node 24, Vite 8, TypeScript 5.9 (`strict`, `noUncheckedIndexedAccess`), Three.js 0.185, Vitest 4 (environnement Node, logique pure seulement).

**Spec:** `docs/superpowers/specs/2026-09-13-wind-design.md` — le plan argumente à partir de la spec ; l'exécutant lit les deux. Contexte : contrat v2 et registre dans `docs/superpowers/specs/2026-09-12-layers-design.md` §3, §7, §10 ; rendu des patches dans `docs/superpowers/specs/2026-09-05-tiles-design.md` §6 ; picking et tooltip dans `docs/superpowers/specs/2026-09-06-navigation-design.md` §3, §5–6.

## Global Constraints

- **Grille** : 1440 × 721, lon −180 → 179,75 (colonne 0 = −180 après `reorient`), lat 90 → −90 (ligne 0 = nord). Pixels CPU (`bitmapPixels`) : RGBA, nord en haut, valeur dans le canal R.
- **Encodage vent** : `Encoding(-60, 60, "linear")` pour `wind_u` et `wind_v` ; `pixel = rint(255·clip((v+60)/120, 0, 1))` ; décodage `v = −60 + 120·(i/255)`. Table de cas partagée Python/TS sans demi-entier : −60 → 0, 60 → 255, 12 → 153, −36 → 51, 30 → 191, −70 → 0, 70 → 255.
- **Registre Python** (ordre = ordre du manifeste et des uploads) : `temp`, `clouds`, `rain`, `pressure`, `humidity`, **`wind_u`**, **`wind_v`**, `pm25`, `dust`. `wind_u` : source `gfs`, variable `UGRD_10m`, unité `m/s`, `nomads_var` `UGRD`, `nomads_lev` `10_m_above_ground`, clés `shortName="10u"`, `typeOfLevel="heightAboveGround"`, `level=10`, conversion identité, plausible brut (−150, 150). `wind_v` : idem avec `VGRD_10m`, `VGRD`, `shortName="10v"`.
- **Legacy retiré** : plus aucun objet `gfs/latest.png` ni `gfs/latest.json` ; `config.LEGACY_*`, `metadata.build_legacy` supprimés. Objets R2 = PNG des couches fraîches (ordre du registre) puis `layers/latest.json` **en dernier**.
- **Simulation** (spec §7) : `WIND_PROFILE = { high: { particles: 12000, trail: 12 }, low: { particles: 3000, trail: 8 } }` ; tick 30 Hz (`TICK_MS = 1000/30`) ; dt borné à 0,1 s ; `P = 2` px/s par m/s ; `speedScale = P · 2·(d−1)·tan(fov/2)/hauteurViewport · 180/π` (degrés par seconde par m/s) ; `dlat = v·S·dt`, `dlon = u·S·dt / cos(lat)` ; respawn si `age > life` (life ∈ [60, 120] ticks), `|lat| > 85°`, `√(u²+v²) < 0,5 m/s`, ou hors vue (horizon puis frustum) ; spawn uniforme en NDC via `pick` injecté, 8 essais, sinon position conservée et `age = 0` ; positions à rayon **1,002** ; tampon slot-major `[K][N][xyz]`, slot 0 = queue, slot K−1 = tête ; respawn écrit les K slots.
- **Rendu** (spec §8) : `LineSegments`, `position` dynamique upload complet, `aAlpha = slot/(K−1)`, index `Uint32` (k, i) → (k+1, i) pour k < K−1, `transparent`, `depthTest: true`, `depthWrite: false`, `renderOrder = 1`, `frustumCulled = false` ; fragment `mix(vec3(1.0), vec3(0.15), uMapStyle)`, alpha `vAlpha · 0.8`, **sans** `#include <colorspace_fragment>` (couleurs données directement en sRGB).
- **État initial** (spec §9) : `?wind=1` → actif, `?wind=0` → inactif, sinon `prefers-reduced-motion: reduce` → inactif, sinon `high` → actif, `low` → inactif. `withWindParam` réécrit seulement `wind`.
- **Tooltip** (spec §10) : « Vent N km/h DIR », `kmh = round(3,6·√(u²+v²))`, `< 1` → « Vent calme », `dir = (270 − atan2(v, u)·180/π) mod 360` (d'où vient le vent), rose N, NNE, NE, ENE, E, ESE, SE, SSE, S, SSO, SO, OSO, O, ONO, NO, NNO (22,5° par point, arrondi au plus proche).
- **Bundle** : `index-*.js` ≤ 150,70 + 12 Ko gzip. 0 draw call au repos vent inactif.
- Commandes depuis la racine : `.venv/Scripts/python -m pytest -q` (Windows : tests GRIB skippés, c'est attendu), `npm --prefix web run test`, `npm --prefix web run typecheck`, `npm --prefix web run build`, `.venv/Scripts/python tools/history_check.py`. Un seul fichier : `.venv/Scripts/python -m pytest tests/pipeline/test_layers.py -q`, `npm --prefix web run test -- tests/wind-sim.test.ts`.
- Branche **`feat/wind`** créée depuis `master`, **en place** (pas de worktree). Pousser la branche dès la T3 (le test de décodage réel ne tourne que sur Actions). Merge final via `superpowers:finishing-a-development-branch`.
- Dépôt en LF, commits en français, Conventional Commits, un commit par tâche au minimum, trailers **littéraux** `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>` et `Claude-Session: https://claude.ai/code/session_0134JMoUqBVX3Am1454qFPP4` (vérifier `git log -1 --format=%B` après chaque commit : ne jamais substituer un autre nom).
- Ne jamais écrire de secret dans le chat ni dans un fichier versionné. Les tests n'appellent jamais le réseau. Ne jamais recommiter `tests/fixtures/gfs_layers.grib2` ni `gefs_chem.grib2`.

---

## Fichiers

| Fichier | Rôle | Tâche |
|---|---|---|
| `pipeline/config.py`, `pipeline/metadata.py`, `pipeline/main.py` | retrait du legacy schema 1 | T1 |
| `tests/pipeline/test_main.py`, `test_metadata.py` | tests sans legacy | T1 |
| `pipeline/layers.py` | `wind_u`, `wind_v` | T2 |
| `tests/pipeline/test_layers.py`, `test_nomads.py`, `test_texture.py`, `test_main.py`, `test_grib_adapter.py`, `web/tests/encoding.test.ts` | registre à 9, URL, table ±60, dry-run 9 couches | T2 |
| `tests/fixtures/gfs_wind.grib2` (nouveau), `tests/pipeline/test_grib_adapter.py` | décodage réel sur Actions | T3 |
| `web/src/wind/select.ts` (nouveau), `web/src/ui/format.ts` | `parseWindParam`, `withWindParam`, `formatWind`, `windDirection`, `compassPoint` | T4 |
| `web/src/wind/sim.ts` (nouveau, partie 1) | `WIND_PROFILE`, constantes, `speedScale`, `isVisible`, `spawnOnScreen` | T5 |
| `web/src/wind/sim.ts` (partie 2) | `WindField`, `WindSim.step` | T6 |
| `web/src/render/wind.ts`, `web/src/render/shaders/wind.vert.glsl`, `wind.frag.glsl` (nouveaux) | `buildTrailIndex`, `buildTrailAlpha`, `createWindLayer` | T7 |
| `web/src/wind/loader.ts` (nouveau), `web/tests/fixtures.ts` | `WindLoader` | T8 |
| `web/src/render/scene.ts`, `web/src/wind/controller.ts` (nouveau) | `onFrame`, `WindController` | T9 |
| `web/index.html`, `web/src/style.css`, `web/src/ui/overlay.ts`, `web/src/ui/wind-toggle.ts` (nouveau), `web/src/ui/tooltip.ts`, `web/src/main.ts` | switch, tooltip deux lignes, conteneur du menu | T10 |
| `web/src/main.ts`, `.gitignore` | câblage, validation navigateur | T11 |
| `HISTORY.md` | §3, §5, §7, §8, §9, pied de page | T12 |

---

### Task 1 : Retrait de `gfs/latest.*` (dette n° 31)

**Files:**
- Modify: `pipeline/config.py:20-27`, `pipeline/metadata.py:1-2,55-70`, `pipeline/main.py:17,143-145`
- Test: `tests/pipeline/test_main.py`, `tests/pipeline/test_metadata.py`

**Interfaces:**
- Consumes: `pipeline.main.run(...)` (inchangé), `pipeline.metadata.build_manifest` (inchangé).
- Produces: `pipeline.config` sans `LEGACY_PNG_KEY`, `LEGACY_JSON_KEY`, `LEGACY_SCHEMA_VERSION` ; `pipeline.metadata` sans `build_legacy` ; `run` publie PNG frais + manifeste seulement.

- [ ] **Step 1 : Créer la branche**

```bash
git checkout master && git pull --ff-only && git checkout -b feat/wind
```

- [ ] **Step 2 : Mettre à jour les tests (échec attendu)**

Dans `tests/pipeline/test_main.py`, renommer et modifier le test nominal :

```python
def test_happy_path_publishes_seven_layers_and_manifest_last(tmp_path):
    code, rec = make_run(tmp_path)
    assert code == EXIT_OK
    assert len(rec.downloads) == 2
    assert "filter_gfs_0p25_1hr" in rec.downloads[0] and "f008" in rec.downloads[0]
    assert "filter_gefs_chem_0p25" in rec.downloads[1] and "f006" in rec.downloads[1]
    keys = [o.key for o in rec.uploads[0]]
    assert keys == [f"layers/{i}.png" for i in GFS_IDS] + [f"layers/{i}.png" for i in CHEM_IDS] + ["layers/latest.json"]
    m = manifest_of(rec)
    assert m["schema_version"] == 2 and list(m["layers"]) == [s.id for s in LAYERS]
    assert m["layers"]["temp"]["run"] == GFS_RUN and m["layers"]["temp"]["forecast_hour"] == GFS_FH
    assert m["layers"]["pm25"]["run"] == CHEM_RUN and m["layers"]["pm25"]["forecast_hour"] == CHEM_FH
    assert m["layers"]["temp"]["stats"] == {"min": 15.0, "max": 15.0}
    assert m["layers"]["rain"]["stats"] == {"min": 3.6, "max": 3.6}
    for o in rec.uploads[0]:
        assert (tmp_path / o.key).read_bytes() == o.body
    assert not (tmp_path / "gfs").exists()


def test_no_legacy_object_is_ever_published(tmp_path):
    code, rec = make_run(tmp_path)
    assert code == EXIT_OK
    assert all(not o.key.startswith("gfs/") for o in rec.uploads[0])
```

Chercher toutes les autres occurrences de `gfs/latest` dans `test_main.py` (`grep -n "gfs/" tests/pipeline/test_main.py`) : retirer `"gfs/latest.png", "gfs/latest.json"` des listes `keys == [...]` attendues (le manifeste reste en dernier) ; renommer `test_only_chem_stale_republishes_chem_without_legacy` en `test_only_chem_stale_republishes_chem_only`.

Dans `tests/pipeline/test_metadata.py` : retirer `build_legacy` de l'import et supprimer `test_build_legacy_is_schema_1_contract` entièrement.

- [ ] **Step 3 : Vérifier l'échec**

Run: `.venv/Scripts/python -m pytest tests/pipeline/test_main.py tests/pipeline/test_metadata.py -q`
Expected: FAIL — `keys == [...]` (le run publie encore `gfs/latest.png`), `assert not (tmp_path / "gfs").exists()`.

- [ ] **Step 4 : Retirer le legacy**

`pipeline/config.py` : supprimer les lignes `LEGACY_PNG_KEY`, `LEGACY_JSON_KEY`, `LEGACY_SCHEMA_VERSION` ; le bloc R2 devient :

```python
# R2 (spec couches §7 ; legacy gfs/latest.* retiré par la spec vent §5)
LAYERS_PREFIX = "layers"
MANIFEST_KEY = "layers/latest.json"
CACHE_CONTROL = "public, max-age=300"

SCHEMA_VERSION = 2
```

`pipeline/metadata.py` : docstring → `"""Manifeste `layers/latest.json` v2 — contrat avec le globe (spec couches §7). Pur."""` ; supprimer la fonction `build_legacy` entière.

`pipeline/main.py` : import → `from pipeline.metadata import build_manifest, iso_utc, layer_entry, to_json` ; supprimer les trois lignes :

```python
    if "temp" in pngs:
        objects.append(Object(config.LEGACY_PNG_KEY, pngs["temp"], "image/png"))
        objects.append(Object(config.LEGACY_JSON_KEY, to_json(build_legacy(entries["temp"])), "application/json"))
```

- [ ] **Step 5 : Vérifier**

Run: `.venv/Scripts/python -m pytest -q`
Expected: PASS (nombre de tests = 173 − 1 + 1 = 173 passed, 9 skipped ; ajuster si le compte diffère, mais **0 failed**). `grep -rn "legacy\|LEGACY" pipeline` ne renvoie plus rien.

- [ ] **Step 6 : Commit**

```bash
git add pipeline/config.py pipeline/metadata.py pipeline/main.py tests/pipeline/test_main.py tests/pipeline/test_metadata.py
git commit -m "refactor(pipeline): retrait du legacy gfs/latest.* schema 1 (dette n° 31)"
```

---

### Task 2 : Registre `wind_u` / `wind_v`

**Files:**
- Modify: `pipeline/layers.py:64-86` (tuple `LAYERS`), `web/src/data/encoding.ts` (aucune modification de code, table de test seulement)
- Test: `tests/pipeline/test_layers.py`, `tests/pipeline/test_nomads.py`, `tests/pipeline/test_texture.py`, `tests/pipeline/test_main.py`, `tests/pipeline/test_grib_adapter.py`, `web/tests/encoding.test.ts`

**Interfaces:**
- Consumes: `LayerSpec`, `Encoding`, `_identity`, `_keys` de `pipeline/layers.py`.
- Produces: `get("wind_u")`, `get("wind_v")` ; `by_source("gfs")` = `["temp", "clouds", "rain", "pressure", "humidity", "wind_u", "wind_v"]` ; manifeste à 9 entrées.

- [ ] **Step 1 : Tests Python (échec attendu)**

`tests/pipeline/test_layers.py` :

```python
def test_nine_layers_in_manifest_order():
    assert [s.id for s in LAYERS] == ["temp", "clouds", "rain", "pressure", "humidity", "wind_u", "wind_v", "pm25", "dust"]


def test_by_source_partitions_registry():
    assert [s.id for s in by_source("gfs")] == ["temp", "clouds", "rain", "pressure", "humidity", "wind_u", "wind_v"]
    assert [s.id for s in by_source("gefs_chem")] == ["pm25", "dust"]
    assert by_source("nope") == []


def test_get_known_and_unknown():
    assert get("temp").variable == "TMP_2m"
    with pytest.raises(KeyError):
        get("gust")


def test_wind_components_share_encoding_and_keys():
    u, v = get("wind_u"), get("wind_v")
    assert (u.encoding.min, u.encoding.max, u.encoding.scale) == (-60, 60, "linear")
    assert u.encoding == v.encoding and u.plausible == v.plausible == (-150.0, 150.0)
    assert (u.nomads_var, u.nomads_lev) == ("UGRD", "10_m_above_ground")
    assert (v.nomads_var, v.nomads_lev) == ("VGRD", "10_m_above_ground")
    assert dict(u.grib_keys) == {"shortName": "10u", "typeOfLevel": "heightAboveGround", "level": 10}
    assert dict(v.grib_keys) == {"shortName": "10v", "typeOfLevel": "heightAboveGround", "level": 10}
    assert (u.variable, u.unit, v.variable) == ("UGRD_10m", "m/s", "VGRD_10m")
```

(Remplacer `test_seven_layers_in_menu_order` par `test_nine_layers_in_manifest_order` ; remplacer les deux tests existants du même nom.) Ajouter à la liste `parametrize` de `test_convert` : `("wind_u", -12.5, -12.5), ("wind_v", 31.0, 31.0),`.

`tests/pipeline/test_nomads.py` — `test_build_url_gfs_all_layers_exact` attend désormais :

```python
    assert url == (
        "https://nomads.ncep.noaa.gov/cgi-bin/filter_gfs_0p25_1hr.pl"
        "?dir=/gfs.20260830/06/atmos&file=gfs.t06z.pgrb2.0p25.f008"
        "&var_TMP=on&var_TCDC=on&var_PRATE=on&var_PRMSL=on&var_RH=on&var_UGRD=on&var_VGRD=on"
        "&lev_2_m_above_ground=on&lev_entire_atmosphere=on&lev_surface=on&lev_mean_sea_level=on&lev_10_m_above_ground=on"
    )
```

`tests/pipeline/test_texture.py` — ajouter `WIND = Encoding(-60, 60, "linear")` sous `SQRT`, et à `ROUNDTRIP_CASES` :

```python
    (WIND, -60.0, 0), (WIND, 60.0, 255), (WIND, 12.0, 153), (WIND, -36.0, 51), (WIND, 30.0, 191), (WIND, -70.0, 0), (WIND, 70.0, 255),
```

et `@pytest.mark.parametrize("enc", [LIN, SQRT, WIND])` sur `test_dequantize_is_inverse_within_one_step`.

`tests/pipeline/test_main.py` : `GFS_IDS = ["temp", "clouds", "rain", "pressure", "humidity", "wind_u", "wind_v"]` ; `RAW` gagne `"wind_u": -3.5, "wind_v": 7.25` ; dans le test nominal ajouter `assert m["layers"]["wind_u"]["stats"] == {"min": -3.5, "max": -3.5}` et `assert m["layers"]["wind_v"]["encoding"] == {"bits": 8, "min": -60, "max": 60, "scale": "linear"}`.

`tests/pipeline/test_grib_adapter.py` : dans `test_decode_all_gfs_layers_from_real_file`, la fixture `gfs_layers.grib2` ne contient pas le vent :

```python
    specs = [s for s in by_source("gfs") if not s.id.startswith("wind_")]  # gfs_layers.grib2 : 5 variables, vent en T3
```

et dans `test_module_importable_without_eccodes` : `assert len(LAYERS) == 9`.

`web/tests/encoding.test.ts` : ajouter au même endroit que les cas `LIN`/`SQRT` existants (même structure de table) :

```ts
const WIND = { bits: 8, min: -60, max: 60, scale: "linear" as const };
// … dans la table partagée avec tests/pipeline/test_texture.py :
[WIND, -60, 0], [WIND, 60, 255], [WIND, 12, 153], [WIND, -36, 51], [WIND, 30, 191], [WIND, -70, 0], [WIND, 70, 255],
```

(Lire le fichier : il itère déjà sur une table `(enc, valeur, pixel)` ; y ajouter ces lignes en respectant sa forme exacte.)

- [ ] **Step 2 : Vérifier l'échec**

Run: `.venv/Scripts/python -m pytest tests/pipeline -q && npm --prefix web run test -- tests/encoding.test.ts`
Expected: FAIL — `KeyError: 'wind_u'`, ordre du registre, URL sans `var_UGRD`.

- [ ] **Step 3 : Ajouter les deux specs**

Dans `pipeline/layers.py`, entre l'entrée `humidity` et le commentaire des aérosols :

```python
    # Vent à 10 m (spec vent §3) : deux composantes scalaires, même encodage signé ; le front
    # les lit par `bitmapPixels` seulement (jamais de texture GPU). ±60 m/s : pas 0,47 m/s.
    LayerSpec("wind_u", "gfs", "UGRD_10m", "m/s", "UGRD", "10_m_above_ground",
              _keys(shortName="10u", typeOfLevel="heightAboveGround", level=10),
              _identity, (-150.0, 150.0), Encoding(-60, 60, "linear")),
    LayerSpec("wind_v", "gfs", "VGRD_10m", "m/s", "VGRD", "10_m_above_ground",
              _keys(shortName="10v", typeOfLevel="heightAboveGround", level=10),
              _identity, (-150.0, 150.0), Encoding(-60, 60, "linear")),
```

- [ ] **Step 4 : Vérifier**

Run: `.venv/Scripts/python -m pytest -q && npm --prefix web run test`
Expected: PASS, 0 failed (skips eccodes/GDAL inchangés).

- [ ] **Step 5 : Commit**

```bash
git add pipeline/layers.py tests/pipeline web/tests/encoding.test.ts
git commit -m "feat(pipeline): couches wind_u/wind_v (UGRD/VGRD 10 m, ±60 m/s linéaire)"
```

---

### Task 3 : Fixture `gfs_wind.grib2` et décodage réel sur Actions

**Files:**
- Create: `tests/fixtures/gfs_wind.grib2`
- Test: `tests/pipeline/test_grib_adapter.py`

**Interfaces:**
- Consumes: `decode_fields(data, specs)`, `list_messages(data)` de `pipeline/grib_adapter.py` ; `get("wind_u")`, `get("wind_v")`.
- Produces: fixture réelle ≈ 1 Mo ; test `test_decode_wind_layers_from_real_file`.

- [ ] **Step 1 : Télécharger la fixture (une fois, à la main)**

Prendre le run 00z de la veille (`date -u -d yesterday +%Y%m%d` sous Git Bash), échéance f003 :

```bash
YMD=$(date -u -d yesterday +%Y%m%d)
curl -fSL -o tests/fixtures/gfs_wind.grib2 "https://nomads.ncep.noaa.gov/cgi-bin/filter_gfs_0p25_1hr.pl?dir=/gfs.${YMD}/00/atmos&file=gfs.t00z.pgrb2.0p25.f003&var_UGRD=on&var_VGRD=on&lev_10_m_above_ground=on"
head -c 4 tests/fixtures/gfs_wind.grib2 ; echo ; ls -l tests/fixtures/gfs_wind.grib2
```

Expected: `GRIB`, taille entre 500 Ko et 2 Mo. Si 403/404 (NOMADS throttle les IP partagées), réessayer après 30 s ou avec le run 06z. **Ne pas** toucher aux deux autres fixtures.

- [ ] **Step 2 : Écrire le test (skippé en local)**

Dans `tests/pipeline/test_grib_adapter.py`, ajouter `WIND_FIXTURE = FIXTURES / "gfs_wind.grib2"` sous `LEGACY_FIXTURE`, puis :

```python
@needs_eccodes
def test_decode_wind_layers_from_real_file():
    from pipeline.grib_adapter import decode_fields, list_messages
    from pipeline.layers import get

    specs = [get("wind_u"), get("wind_v")]
    try:
        fields = decode_fields(WIND_FIXTURE.read_bytes(), specs)
    except Exception as exc:
        pytest.fail(f"{exc}\nMessages du fichier :\n" + "\n".join(map(str, list_messages(WIND_FIXTURE.read_bytes()))))
    assert set(fields) == {"wind_u", "wind_v"}
    for s in specs:
        _assert_gfs_grid(fields[s.id])
        lo, hi = float(fields[s.id].values.min()), float(fields[s.id].values.max())
        assert s.plausible[0] <= lo <= hi <= s.plausible[1], (s.id, lo, hi)
    speed = np.hypot(fields["wind_u"].values, fields["wind_v"].values)
    assert 0 <= float(speed.max()) < 80, float(speed.max())
    assert not np.array_equal(fields["wind_u"].values, fields["wind_v"].values)


def test_wind_fixture_is_a_small_real_grib():
    data = WIND_FIXTURE.read_bytes()
    assert data[:4] == b"GRIB" and 300_000 < len(data) < 3_000_000
```

- [ ] **Step 3 : Vérifier en local**

Run: `.venv/Scripts/python -m pytest tests/pipeline/test_grib_adapter.py -v`
Expected: `test_wind_fixture_is_a_small_real_grib PASSED`, `test_decode_wind_layers_from_real_file SKIPPED` (eccodes indisponible).

- [ ] **Step 4 : Commit et push (la CI exécute le test réel)**

```bash
git add tests/fixtures/gfs_wind.grib2 tests/pipeline/test_grib_adapter.py
git commit -m "test(pipeline): fixture GRIB réelle UGRD/VGRD 10 m et décodage réel du vent"
git push -u origin feat/wind
```

- [ ] **Step 5 : Vérifier la CI**

Run: `gh run list --branch feat/wind --limit 1` puis `gh run watch <id> --exit-status` (ou `gh run view <id> --log-failed`).
Expected: job `test` vert, y compris `test_decode_wind_layers_from_real_file PASSED` et le dry-run NOMADS réel produisant 9 PNG (`out/layers/wind_u.png` visible dans le log « écrit : … 9 couches »). Si `decode_fields` échoue sur les clés, le message liste les messages du fichier : ajuster `grib_keys` dans `layers.py` (ex. `typeOfLevel` réel), recommiter. Le job `web` reste vert.

---

### Task 4 : `wind/select.ts` et `formatWind`

**Files:**
- Create: `web/src/wind/select.ts`
- Modify: `web/src/ui/format.ts`
- Test: `web/tests/wind-select.test.ts` (nouveau), `web/tests/format.test.ts`

**Interfaces:**
- Consumes: `Tier` de `web/src/gpu/tier.ts`.
- Produces: `parseWindParam(search: string, tier: Tier, reducedMotion: boolean): boolean` ; `withWindParam(search: string, on: boolean): string` ; `windDirection(u: number, v: number): number` (degrés d'où vient le vent, [0, 360[) ; `compassPoint(deg: number): string` ; `formatWind(u: number, v: number): string`.

- [ ] **Step 1 : Tests (échec attendu)**

`web/tests/wind-select.test.ts` :

```ts
import { describe, expect, it } from "vitest";
import { parseWindParam, withWindParam } from "../src/wind/select";

describe("parseWindParam — spec vent §9", () => {
  it("?wind=1 et ?wind=0 priment sur tout", () => {
    expect(parseWindParam("?wind=1", "low", true)).toBe(true);
    expect(parseWindParam("?wind=0", "high", false)).toBe(false);
  });
  it("reduced-motion désactive quand l'URL ne dit rien", () => {
    expect(parseWindParam("", "high", true)).toBe(false);
    expect(parseWindParam("?layer=temp", "high", true)).toBe(false);
  });
  it("sinon high → actif, low → inactif", () => {
    expect(parseWindParam("", "high", false)).toBe(true);
    expect(parseWindParam("?layer=rain", "low", false)).toBe(false);
  });
  it("valeur inconnue = absente", () => {
    expect(parseWindParam("?wind=yes", "high", false)).toBe(true);
    expect(parseWindParam("?wind=yes", "low", false)).toBe(false);
  });
});

describe("withWindParam", () => {
  it("réécrit seulement wind et préserve les autres paramètres", () => {
    expect(withWindParam("?layer=rain&lon=1.5&lat=48&d=1.3&wind=1", false)).toBe("?layer=rain&lon=1.5&lat=48&d=1.3&wind=0");
    expect(withWindParam("", true)).toBe("?wind=1");
  });
});
```

Dans `web/tests/format.test.ts`, ajouter (imports depuis `../src/ui/format`) :

```ts
describe("formatWind — spec vent §10", () => {
  it("vitesse en km/h entier, direction d'où vient le vent", () => {
    expect(formatWind(0, 10)).toBe("Vent 36 km/h S");    // souffle vers le nord : vient du sud
    expect(formatWind(-10, 0)).toBe("Vent 36 km/h E");
    expect(formatWind(10, 0)).toBe("Vent 36 km/h O");
    expect(formatWind(0, -10)).toBe("Vent 36 km/h N");
    expect(formatWind(5, 5)).toBe("Vent 25 km/h SO");    // 7,07 m/s = 25,46 km/h
  });
  it("vent calme sous 1 km/h", () => {
    expect(formatWind(0.1, 0)).toBe("Vent calme");
    expect(formatWind(0, 0)).toBe("Vent calme");
  });
  it("windDirection est dans [0, 360[ et compassPoint arrondit au plus proche", () => {
    expect(windDirection(0, 10)).toBeCloseTo(180, 9);
    expect(windDirection(0, -10)).toBeCloseTo(0, 9);
    expect(compassPoint(11.25)).toBe("NNE");
    expect(compassPoint(11.24)).toBe("N");
    expect(compassPoint(348.75)).toBe("N");
    expect(compassPoint(348.74)).toBe("NNO");
    expect(compassPoint(225)).toBe("SO");
  });
});
```

- [ ] **Step 2 : Vérifier l'échec**

Run: `npm --prefix web run test -- tests/wind-select.test.ts tests/format.test.ts`
Expected: FAIL — module `../src/wind/select` introuvable, `formatWind` non exporté.

- [ ] **Step 3 : Implémenter**

`web/src/wind/select.ts` :

```ts
/** Logique pure de l'interrupteur vent (spec vent §9) : `?wind=`, défaut par tier et reduced-motion. */
import type { Tier } from "../gpu/tier";

const PARAM = "wind";

export function parseWindParam(search: string, tier: Tier, reducedMotion: boolean): boolean {
  const raw = new URLSearchParams(search).get(PARAM);
  if (raw === "1") return true;
  if (raw === "0") return false;
  if (reducedMotion) return false;
  return tier === "high";
}

/** Réécrit seulement `wind` dans la query string. Résultat préfixé par `?`. */
export function withWindParam(search: string, on: boolean): string {
  const p = new URLSearchParams(search);
  p.set(PARAM, on ? "1" : "0");
  return `?${p.toString()}`;
}
```

À la fin de `web/src/ui/format.ts` :

```ts
const COMPASS = ["N", "NNE", "NE", "ENE", "E", "ESE", "SE", "SSE", "S", "SSO", "SO", "OSO", "O", "ONO", "NO", "NNO"] as const;

/** Direction météo (d'où vient le vent), degrés dans [0, 360[ : (270 − atan2(v, u)) mod 360. */
export function windDirection(u: number, v: number): number {
  const d = 270 - (Math.atan2(v, u) * 180) / Math.PI;
  return ((d % 360) + 360) % 360;
}

/** Point de rose le plus proche (16 points, 22,5° chacun, N centré sur 0°). */
export function compassPoint(deg: number): string {
  return COMPASS[Math.round(deg / 22.5) % 16]!;
}

/** Ligne vent du tooltip (spec vent §10) : « Vent 23 km/h NO », « Vent calme » sous 1 km/h. */
export function formatWind(u: number, v: number): string {
  const kmh = Math.round(3.6 * Math.hypot(u, v));
  if (kmh < 1) return "Vent calme";
  return `Vent ${kmh} km/h ${compassPoint(windDirection(u, v))}`;
}
```

- [ ] **Step 4 : Vérifier**

Run: `npm --prefix web run test -- tests/wind-select.test.ts tests/format.test.ts && npm --prefix web run typecheck`
Expected: PASS.

- [ ] **Step 5 : Commit**

```bash
git add web/src/wind/select.ts web/src/ui/format.ts web/tests/wind-select.test.ts web/tests/format.test.ts
git commit -m "feat(web): paramètre ?wind, défaut par tier, formatWind km/h + rose des vents"
```

---

### Task 5 : `wind/sim.ts` — profils, échelle de vitesse, visibilité, spawn

**Files:**
- Create: `web/src/wind/sim.ts`
- Test: `web/tests/wind-sim.test.ts` (nouveau)

**Interfaces:**
- Consumes: `ViewState` de `web/src/tiles/lod.ts` ; `THREE.Vector3`, `THREE.Frustum`.
- Produces:
  - `WIND_PROFILE: Record<Tier, { particles: number; trail: number }>`, `TICK_MS`, `MAX_DT_S = 0.1`, `PX_PER_S_PER_MS = 2`, `RADIUS = 1.002`, `MAX_LAT = 85`, `MIN_SPEED = 0.5`, `LIFE_MIN = 60`, `LIFE_MAX = 120`, `SPAWN_TRIES = 8` ;
  - `speedScale(view: Pick<ViewState, "cameraPosition" | "fovYRad" | "viewportHeight">, pxPerSecPerMs?: number): number` ;
  - `isVisible(p: THREE.Vector3, view: Pick<ViewState, "cameraPosition" | "frustum">): boolean` ;
  - `type PickFn = (ndcX: number, ndcY: number, target: THREE.Vector3) => THREE.Vector3 | null` ;
  - `spawnOnScreen(pick: PickFn, rng: () => number, target: THREE.Vector3): boolean`.

- [ ] **Step 1 : Tests (échec attendu)**

`web/tests/wind-sim.test.ts` :

```ts
import * as THREE from "three";
import { describe, expect, it, vi } from "vitest";
import { viewStateFrom } from "../src/tiles/lod";
import { lonLatToVec3 } from "../src/tiles/patch";
import {
  MAX_LAT, PX_PER_S_PER_MS, SPAWN_TRIES, WIND_PROFILE, isVisible, spawnOnScreen, speedScale, type PickFn,
} from "../src/wind/sim";

/** Caméra à la distance d sur +z, viewport carré, fov 45°. */
export function view(d: number, height = 800) {
  const camera = new THREE.PerspectiveCamera(45, 1, 0.01, 10);
  camera.position.set(0, 0, d);
  camera.lookAt(0, 0, 0);
  camera.updateMatrixWorld(true);
  camera.updateProjectionMatrix();
  return viewStateFrom(camera, height);
}

describe("WIND_PROFILE", () => {
  it("budgets de la spec §7", () => {
    expect(WIND_PROFILE.high).toEqual({ particles: 12000, trail: 12 });
    expect(WIND_PROFILE.low).toEqual({ particles: 3000, trail: 8 });
    expect(MAX_LAT).toBe(85);
  });
});

describe("speedScale — vitesse apparente constante", () => {
  for (const d of [4, 1.1]) {
    it(`20 m/s → 40 px/s à d = ${d}`, () => {
      const v = view(d);
      const worldPerPx = (2 * (d - 1) * Math.tan(v.fovYRad / 2)) / v.viewportHeight;
      const degPerS = speedScale(v) * 20;
      const pxPerS = ((degPerS * Math.PI) / 180) / worldPerPx;
      expect(pxPerS).toBeCloseTo(20 * PX_PER_S_PER_MS, 6);
    });
  }
  it("P injectable", () => {
    expect(speedScale(view(4), 4)).toBeCloseTo(2 * speedScale(view(4)), 9);
  });
});

describe("isVisible", () => {
  const v = view(3);
  it("point face caméra visible, face cachée invisible", () => {
    expect(isVisible(new THREE.Vector3(0, 0, 1.002), v)).toBe(true);
    expect(isVisible(new THREE.Vector3(0, 0, -1.002), v)).toBe(false);
  });
  it("point derrière l'horizon (dot < 1/d) invisible même dans le frustum", () => {
    // d = 3 : horizon à cos θ = 1/3 ; z = 0.3 est devant le limbe géométrique mais derrière l'horizon
    expect(isVisible(new THREE.Vector3(Math.sqrt(1 - 0.09), 0, 0.3), v)).toBe(false);
  });
  it("point devant l'horizon mais hors frustum invisible", () => {
    // d = 1,05 : demi-hauteur visible à la surface ≈ 0,05·tan(22,5°) = 0,021 ; y = 0,1 sort du champ, dot = 0,995 > 1/1,05
    expect(isVisible(new THREE.Vector3(0, 0.1, Math.sqrt(1 - 0.01)), view(1.05))).toBe(false);
    expect(isVisible(new THREE.Vector3(0, 0.01, Math.sqrt(1 - 0.0001)), view(1.05))).toBe(true);
  });
});

describe("spawnOnScreen", () => {
  it("tire en NDC dans [-1, 1] et accepte le premier point trouvé", () => {
    const calls: [number, number][] = [];
    const pick: PickFn = (x, y, t) => { calls.push([x, y]); return t.set(x, y, 0); };
    const seq = [0.25, 0.75];
    let i = 0;
    const rng = () => seq[i++ % 2]!;
    const target = new THREE.Vector3();
    expect(spawnOnScreen(pick, rng, target)).toBe(true);
    expect(calls).toEqual([[-0.5, 0.5]]);
    expect(target.x).toBeCloseTo(-0.5, 9);
  });
  it("8 essais puis abandon", () => {
    const pick = vi.fn<PickFn>(() => null);
    expect(spawnOnScreen(pick, Math.random, new THREE.Vector3())).toBe(false);
    expect(pick).toHaveBeenCalledTimes(SPAWN_TRIES);
  });
  it("réussit au dernier essai", () => {
    let n = 0;
    const pick: PickFn = (_x, _y, t) => (++n < SPAWN_TRIES ? null : t.set(0, 0, 1));
    expect(spawnOnScreen(pick, Math.random, new THREE.Vector3())).toBe(true);
    expect(n).toBe(SPAWN_TRIES);
  });
});
```

- [ ] **Step 2 : Vérifier l'échec**

Run: `npm --prefix web run test -- tests/wind-sim.test.ts`
Expected: FAIL — module introuvable.

- [ ] **Step 3 : Implémenter la partie 1 de `sim.ts`**

```ts
/**
 * Simulation des particules de vent (spec vent §7). Logique pure : aucun DOM, aucun GPU.
 * Les positions vivent dans un tampon slot-major [K][N][xyz] consommé tel quel par
 * render/wind.ts (slot 0 = queue, slot K−1 = tête).
 */
import * as THREE from "three";
import type { Tier } from "../gpu/tier";
import type { ViewState } from "../tiles/lod";

export const WIND_PROFILE: Record<Tier, { particles: number; trail: number }> = {
  high: { particles: 12_000, trail: 12 },
  low: { particles: 3_000, trail: 8 },
};

export const TICK_MS = 1000 / 30;
export const MAX_DT_S = 0.1;
/** Vitesse apparente : px CSS par seconde et par m/s (20 m/s → 40 px/s). */
export const PX_PER_S_PER_MS = 2;
export const RADIUS = 1.002;
export const MAX_LAT = 85;
export const MIN_SPEED = 0.5;
export const LIFE_MIN = 60;
export const LIFE_MAX = 120;
export const SPAWN_TRIES = 8;

const DEG = Math.PI / 180;

export type PickFn = (ndcX: number, ndcY: number, target: THREE.Vector3) => THREE.Vector3 | null;

/** Degrés par seconde par m/s tels que la vitesse à l'écran ne dépende pas de l'altitude. */
export function speedScale(
  view: Pick<ViewState, "cameraPosition" | "fovYRad" | "viewportHeight">,
  pxPerSecPerMs: number = PX_PER_S_PER_MS,
): number {
  const d = view.cameraPosition.length();
  const worldPerPx = (2 * (d - 1) * Math.tan(view.fovYRad / 2)) / view.viewportHeight;
  return pxPerSecPerMs * worldPerPx * (180 / Math.PI);
}

const camDir = new THREE.Vector3();

/** Devant l'horizon (dot(P̂, Ĉ) > 1/|C|, spec tuiles §5) et dans le frustum. */
export function isVisible(p: THREE.Vector3, view: Pick<ViewState, "cameraPosition" | "frustum">): boolean {
  const d = view.cameraPosition.length();
  camDir.copy(view.cameraPosition).divideScalar(d);
  if (p.dot(camDir) / (p.length() || 1) <= 1 / d) return false;
  return view.frustum.containsPoint(p);
}

/** Tirage uniforme à l'écran : (x, y) en NDC, projeté sur la sphère par `pick`. `false` après SPAWN_TRIES ratés. */
export function spawnOnScreen(pick: PickFn, rng: () => number, target: THREE.Vector3): boolean {
  for (let i = 0; i < SPAWN_TRIES; i++) {
    const x = rng() * 2 - 1;
    const y = rng() * 2 - 1;
    if (pick(x, y, target)) return true;
  }
  return false;
}
```

- [ ] **Step 4 : Vérifier**

Run: `npm --prefix web run test -- tests/wind-sim.test.ts && npm --prefix web run typecheck`
Expected: PASS.

- [ ] **Step 5 : Commit**

```bash
git add web/src/wind/sim.ts web/tests/wind-sim.test.ts
git commit -m "feat(web): wind/sim — profils par tier, échelle de vitesse, visibilité, spawn écran"
```

---

### Task 6 : `WindSim.step` — advection, respawn, tampon

**Files:**
- Modify: `web/src/wind/sim.ts`
- Test: `web/tests/wind-sim.test.ts`

**Interfaces:**
- Consumes: `sampleValue` de `web/src/data/sampling.ts` ; `lonLatToVec3` de `web/src/tiles/patch.ts` ; `vec3ToLonLat` de `web/src/render/pick.ts` ; `Encoding` de `web/src/data/encoding.ts` ; `Grid` de `web/src/data/manifest.ts` ; helpers de la T5.
- Produces:
  - `interface WindField { u: Uint8ClampedArray; v: Uint8ClampedArray; grid: Pick<Grid, "width" | "height">; encU: Encoding; encV: Encoding }` ;
  - `class WindSim { constructor(count: number, trail: number); readonly count; readonly trail; readonly positions: Float32Array; readonly lon: Float32Array; readonly lat: Float32Array; readonly age: Uint16Array; readonly life: Uint16Array; step(field: WindField, dtS: number, view: ViewState, pick: PickFn, rng?: () => number): void }`.

- [ ] **Step 1 : Tests (échec attendu)**

Ajouter à `web/tests/wind-sim.test.ts` (imports supplémentaires : `encode` de `../src/data/encoding`, `WindSim, RADIUS, MIN_SPEED, LIFE_MIN, LIFE_MAX, type WindField` de `../src/wind/sim`, `vec3ToLonLat` de `../src/render/pick`) :

```ts
const GRID = { width: 8, height: 5 };
const ENC = { bits: 8, min: -60, max: 60, scale: "linear" as const };

/** Champ constant (u, v) en m/s sur une petite grille : sampleValue renvoie la constante partout. */
function field(u: number, v: number): WindField {
  const fill = (val: number) => {
    const px = new Uint8ClampedArray(GRID.width * GRID.height * 4);
    const b = encode(val, ENC);
    for (let i = 0; i < px.length; i += 4) { px[i] = b; px[i + 3] = 255; }
    return px;
  };
  return { u: fill(u), v: fill(v), grid: GRID, encU: ENC, encV: ENC };
}

/** Pick qui pose toujours la particule au même point (lon, lat) visible depuis +z. */
function pickAt(lon: number, lat: number): PickFn {
  return (_x, _y, t) => lonLatToVec3(lon, lat, t);
}
const DT = 1 / 30;
const rng0 = () => 0; // life = LIFE_MIN

function slot(sim: WindSim, k: number, i: number) {
  const o = (k * sim.count + i) * 3;
  return new THREE.Vector3(sim.positions[o], sim.positions[o + 1], sim.positions[o + 2]);
}

describe("WindSim.step — advection", () => {
  it("premier tick : toutes les particules naissent au point du pick, K slots égaux, rayon 1,002", () => {
    const sim = new WindSim(2, 3);
    sim.step(field(0, 0), DT, view(3), pickAt(-100, 20), rng0);
    for (let i = 0; i < 2; i++) {
      expect(sim.lon[i]).toBeCloseTo(-100, 5);
      expect(sim.lat[i]).toBeCloseTo(20, 5);
      for (let k = 0; k < 3; k++) expect(slot(sim, k, i).length()).toBeCloseTo(RADIUS, 9);
      expect(slot(sim, 0, i).distanceTo(slot(sim, 2, i))).toBe(0);
      expect(sim.age[i]).toBe(0);
      expect(sim.life[i]).toBe(LIFE_MIN);
    }
  });
  it("v > 0 fait croître la latitude de v·S·dt ; u = 0 laisse la longitude", () => {
    const sim = new WindSim(1, 3);
    const v = view(3);
    sim.step(field(0, 0), DT, v, pickAt(-100, 20), rng0);
    sim.step(field(0, 10), DT, v, pickAt(-100, 20), rng0);
    expect(sim.lat[0]).toBeCloseTo(20 + 10 * speedScale(v) * DT, 5);
    expect(sim.lon[0]).toBeCloseTo(-100, 5);
    expect(sim.age[0]).toBe(1);
  });
  it("dlon est divisé par cos(lat) : même u, déplacement double à 60° qu'à 0°", () => {
    const v = view(3);
    const at = (lat: number) => {
      const sim = new WindSim(1, 3);
      sim.step(field(0, 0), DT, v, pickAt(-100, lat), rng0);
      sim.step(field(10, 0), DT, v, pickAt(-100, lat), rng0);
      return sim.lon[0]! + 100;
    };
    expect(at(60) / at(0)).toBeCloseTo(2, 3);
  });
  it("bouclage antiméridien : 179,9° + est → −179,…", () => {
    const camera = new THREE.PerspectiveCamera(45, 1, 0.01, 10);
    camera.position.set(-3, 0, 0);
    camera.lookAt(0, 0, 0);
    camera.updateMatrixWorld(true);
    camera.updateProjectionMatrix();
    const v = viewStateFrom(camera, 800);
    const sim = new WindSim(1, 3);
    sim.step(field(0, 0), DT, v, pickAt(179.9, 0), rng0);
    sim.step(field(60, 0), DT, v, pickAt(179.9, 0), rng0);
    expect(sim.lon[0]).toBeLessThan(-179);
    expect(sim.lon[0]).toBeGreaterThan(-180);
  });
  it("le tampon glisse : slot K−1 = position nouvelle, slots précédents = anciennes", () => {
    const sim = new WindSim(1, 3);
    const v = view(3);
    sim.step(field(0, 0), DT, v, pickAt(-100, 20), rng0);
    const p0 = slot(sim, 2, 0);
    sim.step(field(0, 10), DT, v, pickAt(-100, 20), rng0);
    const p1 = slot(sim, 2, 0);
    expect(p1.distanceTo(p0)).toBeGreaterThan(0);
    expect(slot(sim, 1, 0).distanceTo(p0)).toBe(0);
    expect(slot(sim, 0, 0).distanceTo(p0)).toBe(0);
    sim.step(field(0, 10), DT, v, pickAt(-100, 20), rng0);
    expect(slot(sim, 0, 0).distanceTo(p0)).toBe(0);
    expect(slot(sim, 1, 0).distanceTo(p1)).toBe(0);
    expect(slot(sim, 2, 0).distanceTo(p1)).toBeGreaterThan(0);
  });
  it("dt borné à 0,1 s", () => {
    const sim = new WindSim(1, 3);
    const v = view(3);
    sim.step(field(0, 0), DT, v, pickAt(-100, 20), rng0);
    sim.step(field(0, 10), 5, v, pickAt(-100, 20), rng0);
    expect(sim.lat[0]).toBeCloseTo(20 + 10 * speedScale(v) * 0.1, 5);
  });
});

describe("WindSim.step — respawn", () => {
  const spawnThenExpectRespawn = (mutate: (sim: WindSim) => void, f: WindField = field(0, 10)) => {
    const sim = new WindSim(1, 3);
    const v = view(3);
    sim.step(field(0, 0), DT, v, pickAt(-100, 20), rng0);
    mutate(sim);
    const pick = vi.fn<PickFn>(pickAt(-100, 20));
    sim.step(f, DT, v, pick, rng0);
    expect(pick).toHaveBeenCalledTimes(1);
    expect(sim.lon[0]).toBeCloseTo(-100, 5);
    expect(sim.lat[0]).toBeCloseTo(20, 5);
    expect(sim.age[0]).toBe(0);
    for (let k = 0; k < 3; k++) expect(slot(sim, k, 0).distanceTo(slot(sim, 2, 0))).toBe(0);
  };
  it("age > life", () => spawnThenExpectRespawn((s) => { s.age[0] = 200; }));
  it("|lat| > 85°", () => spawnThenExpectRespawn((s) => { s.lat[0] = 86; }));
  it("sortie de la vue (face cachée)", () => spawnThenExpectRespawn((s) => { s.lon[0] = 80; }));
  it("vent < 0,5 m/s (figée)", () => spawnThenExpectRespawn(() => {}, field(0.2, 0.2)));
  it("pick raté 8 fois : position conservée, age remis à 0", () => {
    const sim = new WindSim(1, 3);
    const v = view(3);
    sim.step(field(0, 0), DT, v, pickAt(-100, 20), rng0);
    sim.age[0] = 200;
    const pick = vi.fn<PickFn>(() => null);
    sim.step(field(0, 10), DT, v, pick, rng0);
    expect(pick).toHaveBeenCalledTimes(SPAWN_TRIES);
    expect(sim.lon[0]).toBeCloseTo(-100, 5);
    expect(sim.lat[0]).toBeCloseTo(20, 5);
    expect(sim.age[0]).toBe(0);
    expect(slot(sim, 2, 0).length()).toBeCloseTo(RADIUS, 9);
  });
  it("life tirée dans [60, 120]", () => {
    const sim = new WindSim(1, 3);
    sim.step(field(0, 0), DT, view(3), pickAt(-100, 20), () => 0.999999);
    expect(sim.life[0]).toBe(LIFE_MAX);
  });
  it("vitesse inférieure à MIN_SPEED constatée", () => {
    expect(Math.hypot(0.2, 0.2)).toBeLessThan(MIN_SPEED);
  });
});
```

- [ ] **Step 2 : Vérifier l'échec**

Run: `npm --prefix web run test -- tests/wind-sim.test.ts`
Expected: FAIL — `WindSim` non exporté.

- [ ] **Step 3 : Implémenter `WindField` et `WindSim`**

Ajouter à `web/src/wind/sim.ts` (imports en tête : `import { sampleValue } from "../data/sampling"; import type { Encoding } from "../data/encoding"; import type { Grid } from "../data/manifest"; import { lonLatToVec3 } from "../tiles/patch"; import { vec3ToLonLat } from "../render/pick";`) :

```ts
export interface WindField {
  u: Uint8ClampedArray;
  v: Uint8ClampedArray;
  grid: Pick<Grid, "width" | "height">;
  encU: Encoding;
  encV: Encoding;
}

const tmp = new THREE.Vector3();

export class WindSim {
  /** Slot-major [K][N][xyz], rayon RADIUS. Lu par render/wind.ts sans copie. */
  readonly positions: Float32Array;
  readonly lon: Float32Array;
  readonly lat: Float32Array;
  readonly age: Uint16Array;
  readonly life: Uint16Array;

  constructor(readonly count: number, readonly trail: number) {
    if (!(count >= 1) || !(trail >= 2)) throw new Error("count ≥ 1 et trail ≥ 2 attendus");
    this.positions = new Float32Array(trail * count * 3);
    this.lon = new Float32Array(count);
    this.lat = new Float32Array(count);
    this.age = new Uint16Array(count).fill(1);   // age > life = 0 : toutes naissent au premier tick
    this.life = new Uint16Array(count);
  }

  private write(slot: number, i: number, p: THREE.Vector3): void {
    const o = (slot * this.count + i) * 3;
    this.positions[o] = p.x;
    this.positions[o + 1] = p.y;
    this.positions[o + 2] = p.z;
  }

  step(field: WindField, dtS: number, view: ViewState, pick: PickFn, rng: () => number = Math.random): void {
    const dt = Math.min(dtS, MAX_DT_S);
    const N = this.count;
    const K = this.trail;
    const head = K - 1;
    this.positions.copyWithin(0, N * 3); // slots 1…K−1 → 0…K−2
    const S = speedScale(view);
    for (let i = 0; i < N; i++) {
      let lon = this.lon[i]!;
      let lat = this.lat[i]!;
      let respawn = this.age[i]! > this.life[i]! || Math.abs(lat) > MAX_LAT;
      if (!respawn) {
        const u = sampleValue(field.u, field.grid, field.encU, lon, lat);
        const v = sampleValue(field.v, field.grid, field.encV, lon, lat);
        if (Math.hypot(u, v) < MIN_SPEED) {
          respawn = true;
        } else {
          lon += (u * S * dt) / Math.cos(lat * DEG);
          lat += v * S * dt;
          if (lon >= 180) lon -= 360;
          else if (lon < -180) lon += 360;
          lonLatToVec3(lon, lat, tmp).multiplyScalar(RADIUS);
          respawn = Math.abs(lat) > MAX_LAT || !isVisible(tmp, view);
        }
      }
      if (respawn) {
        this.age[i] = 0;
        if (spawnOnScreen(pick, rng, tmp)) {
          const ll = vec3ToLonLat(tmp);
          lon = ll.lon;
          lat = ll.lat;
          this.life[i] = LIFE_MIN + Math.floor(rng() * (LIFE_MAX - LIFE_MIN + 1));
          tmp.normalize().multiplyScalar(RADIUS);
          for (let k = 0; k < K; k++) this.write(k, i, tmp);
        } else {
          lonLatToVec3(lon, lat, tmp).multiplyScalar(RADIUS); // position conservée
          this.write(head, i, tmp);
        }
      } else {
        this.age[i]!++;
        this.write(head, i, tmp);
      }
      this.lon[i] = lon;
      this.lat[i] = lat;
    }
  }
}
```

Note `this.age[i]!++` : avec `noUncheckedIndexedAccess`, écrire `this.age[i] = this.age[i]! + 1` si TypeScript refuse la forme postfixée.

- [ ] **Step 4 : Vérifier**

Run: `npm --prefix web run test -- tests/wind-sim.test.ts && npm --prefix web run typecheck`
Expected: PASS. Le test « life tirée » avec `rng = 0.999999` donne `60 + floor(0.999999 × 61) = 120`.

- [ ] **Step 5 : Mesure du coût CPU (information, pas un test)**

Ajouter temporairement dans le test, puis retirer avant commit :

```ts
it.skip("coût d'un tick high (information)", () => {
  const sim = new WindSim(12000, 12);
  const v = view(3);
  const f = field(5, 5);
  sim.step(f, DT, v, pickAt(-100, 20), Math.random);
  const t0 = performance.now();
  for (let n = 0; n < 30; n++) sim.step(f, DT, v, pickAt(-100, 20), Math.random);
  console.log("tick high ms", (performance.now() - t0) / 30);
});
```

Noter la valeur dans le message de commit (attendu : < 4 ms sur un desktop récent ; > 8 ms = alerter avant de continuer, le critère 8 de la spec est en jeu).

- [ ] **Step 6 : Commit**

```bash
git add web/src/wind/sim.ts web/tests/wind-sim.test.ts
git commit -m "feat(web): WindSim — advection U/V, respawn, tampon slot-major glissant"
```

---

### Task 7 : `render/wind.ts` — géométrie, matériau, shaders

**Files:**
- Create: `web/src/render/wind.ts`, `web/src/render/shaders/wind.vert.glsl`, `web/src/render/shaders/wind.frag.glsl`
- Test: `web/tests/wind-layer.test.ts` (nouveau)

**Interfaces:**
- Consumes: `THREE.LineSegments`, `THREE.BufferGeometry`, `THREE.BufferAttribute`, `THREE.ShaderMaterial` ; tampon `positions` de `WindSim`.
- Produces:
  - `buildTrailIndex(count: number, trail: number): Uint32Array` ;
  - `buildTrailAlpha(count: number, trail: number): Float32Array` ;
  - `interface WindLayer { object: THREE.LineSegments; setMapStyle(v: number): void; markDirty(): void; dispose(): void }` ;
  - `createWindLayer(positions: Float32Array, count: number, trail: number): WindLayer`.

- [ ] **Step 1 : Tests (échec attendu)**

`web/tests/wind-layer.test.ts` :

```ts
import * as THREE from "three";
import { describe, expect, it } from "vitest";
import { buildTrailAlpha, buildTrailIndex, createWindLayer } from "../src/render/wind";

describe("buildTrailIndex / buildTrailAlpha — spec vent §8", () => {
  it("N·(K−1) segments (k, i) → (k+1, i), aucun segment de bouclage", () => {
    const idx = buildTrailIndex(3, 4);
    expect(idx).toBeInstanceOf(Uint32Array);
    expect(idx.length).toBe(3 * 3 * 2);
    expect([...idx.slice(0, 2)]).toEqual([0, 3]);           // slot 0 → slot 1, particule 0
    expect([...idx.slice(-2)]).toEqual([2 * 3 + 2, 3 * 3 + 2]); // slot 2 → slot 3, particule 2
    for (let s = 0; s < idx.length; s += 2) expect(idx[s + 1]! - idx[s]!).toBe(3);
  });
  it("alpha 0 à la queue, 1 à la tête, linéaire", () => {
    const a = buildTrailAlpha(2, 5);
    expect(a.length).toBe(10);
    expect(a[0]).toBe(0);
    expect(a[1]).toBe(0);
    expect(a[4]).toBeCloseTo(0.5, 9);
    expect(a[8]).toBe(1);
    expect(a[9]).toBe(1);
  });
});

describe("createWindLayer", () => {
  const positions = new Float32Array(4 * 3 * 3);
  const layer = createWindLayer(positions, 3, 4);
  it("LineSegments transparent, sans écriture de profondeur, dessiné après le globe, jamais cullé", () => {
    expect(layer.object).toBeInstanceOf(THREE.LineSegments);
    const m = layer.object.material as THREE.ShaderMaterial;
    expect(m.transparent).toBe(true);
    expect(m.depthWrite).toBe(false);
    expect(m.depthTest).toBe(true);
    expect(layer.object.renderOrder).toBe(1);
    expect(layer.object.frustumCulled).toBe(false);
    expect(layer.object.visible).toBe(false);
  });
  it("partage le tampon de positions sans copie, usage dynamique", () => {
    const pos = layer.object.geometry.getAttribute("position") as THREE.BufferAttribute;
    expect(pos.array).toBe(positions);
    expect(pos.itemSize).toBe(3);
    expect(pos.usage).toBe(THREE.DynamicDrawUsage);
    expect(layer.object.geometry.getAttribute("aAlpha").count).toBe(12);
    expect(layer.object.geometry.index!.count).toBe(3 * 3 * 2);
  });
  it("markDirty signale l'attribut ; setMapStyle pose l'uniform", () => {
    const pos = layer.object.geometry.getAttribute("position") as THREE.BufferAttribute;
    const before = pos.version;
    layer.markDirty();
    expect(pos.version).toBe(before + 1);
    layer.setMapStyle(0.5);
    expect((layer.object.material as THREE.ShaderMaterial).uniforms.uMapStyle!.value).toBe(0.5);
  });
});
```

- [ ] **Step 2 : Vérifier l'échec**

Run: `npm --prefix web run test -- tests/wind-layer.test.ts`
Expected: FAIL — module introuvable.

- [ ] **Step 3 : Shaders**

`web/src/render/shaders/wind.vert.glsl` :

```glsl
// Traînées de vent (spec vent §8) : positions déjà sur la sphère (rayon 1,002), alpha statique par slot.
attribute float aAlpha;
varying float vAlpha;

void main() {
  vAlpha = aAlpha;
  gl_Position = projectionMatrix * modelViewMatrix * vec4(position, 1.0);
}
```

`web/src/render/shaders/wind.frag.glsl` :

```glsl
// Blanc sur satellite, gris foncé sur style carte (même uMapStyle que patch.frag.glsl).
// Pas de colorspace_fragment : ces gris sont donnés directement en sRGB.
uniform float uMapStyle;
varying float vAlpha;

void main() {
  vec3 color = mix(vec3(1.0), vec3(0.15), uMapStyle);
  gl_FragColor = vec4(color, vAlpha * 0.8);
}
```

- [ ] **Step 4 : Implémenter `render/wind.ts`**

```ts
/**
 * Rendu des traînées de vent (spec vent §8) : un seul LineSegments sur le tampon slot-major
 * [K][N][xyz] de wind/sim.ts. Index et alpha sont statiques ; seules les positions changent,
 * envoyées entières à chaque tick (markDirty).
 */
import * as THREE from "three";
import fragmentShader from "./shaders/wind.frag.glsl?raw";
import vertexShader from "./shaders/wind.vert.glsl?raw";

export interface WindLayer {
  object: THREE.LineSegments;
  /** 0 = satellite (blanc), 1 = carte (gris foncé) ; même valeur que le globe. */
  setMapStyle(v: number): void;
  /** À appeler après chaque tick de simulation. */
  markDirty(): void;
  dispose(): void;
}

/** Segments (k, i) → (k+1, i) pour k < K−1 ; sommet (k, i) = k·N + i. */
export function buildTrailIndex(count: number, trail: number): Uint32Array {
  const idx = new Uint32Array(count * (trail - 1) * 2);
  let o = 0;
  for (let k = 0; k < trail - 1; k++) {
    for (let i = 0; i < count; i++) {
      idx[o++] = k * count + i;
      idx[o++] = (k + 1) * count + i;
    }
  }
  return idx;
}

/** Alpha du sommet (k, i) = k / (K−1) : 0 à la queue, 1 à la tête. */
export function buildTrailAlpha(count: number, trail: number): Float32Array {
  const a = new Float32Array(count * trail);
  for (let k = 0; k < trail; k++) a.fill(k / (trail - 1), k * count, (k + 1) * count);
  return a;
}

export function createWindLayer(positions: Float32Array, count: number, trail: number): WindLayer {
  if (positions.length !== count * trail * 3) throw new Error("tampon de positions de taille inattendue");
  const geometry = new THREE.BufferGeometry();
  const position = new THREE.BufferAttribute(positions, 3);
  position.setUsage(THREE.DynamicDrawUsage);
  geometry.setAttribute("position", position);
  geometry.setAttribute("aAlpha", new THREE.BufferAttribute(buildTrailAlpha(count, trail), 1));
  geometry.setIndex(new THREE.BufferAttribute(buildTrailIndex(count, trail), 1));
  const uniforms = { uMapStyle: { value: 0 } };
  const material = new THREE.ShaderMaterial({
    uniforms, vertexShader, fragmentShader,
    transparent: true, depthWrite: false, depthTest: true,
  });
  const object = new THREE.LineSegments(geometry, material);
  object.frustumCulled = false;
  object.matrixAutoUpdate = false;
  object.renderOrder = 1;
  object.visible = false;
  return {
    object,
    setMapStyle(v) {
      uniforms.uMapStyle.value = v;
    },
    markDirty() {
      position.needsUpdate = true;
    },
    dispose() {
      geometry.dispose();
      material.dispose();
    },
  };
}
```

- [ ] **Step 5 : Vérifier**

Run: `npm --prefix web run test -- tests/wind-layer.test.ts && npm --prefix web run typecheck`
Expected: PASS. (Vitest résout `?raw` comme Vite ; `tiles-globe.test.ts` importe déjà des shaders ainsi.)

- [ ] **Step 6 : Commit**

```bash
git add web/src/render/wind.ts web/src/render/shaders/wind.vert.glsl web/src/render/shaders/wind.frag.glsl web/tests/wind-layer.test.ts
git commit -m "feat(web): render/wind — LineSegments des traînées, alpha par slot, shaders"
```

---

### Task 8 : `wind/loader.ts` — `WindLoader`

**Files:**
- Create: `web/src/wind/loader.ts`
- Modify: `web/tests/fixtures.ts`
- Test: `web/tests/wind-loader.test.ts` (nouveau)

**Interfaces:**
- Consumes: `browserDeps`, `textureUrl`, `needsTextureFetch`, `TextureError` de `web/src/data/loader.ts` ; `LayerEntry`, `Grid` de `web/src/data/manifest.ts` ; `WindField` de `web/src/wind/sim.ts`.
- Produces:
  - `interface WindLoaderDeps { fetchBitmap(url: string): Promise<ImageBitmap>; bitmapPixels(b: ImageBitmap): Uint8ClampedArray | null }` ;
  - `class WindLoader { constructor(baseUrl: string, deps?: WindLoaderDeps); get field(): WindField | null; load(entryU: LayerEntry, entryV: LayerEntry, grid: Pick<Grid, "width" | "height">): Promise<WindField | null>; dispose(): void }` ;
  - `WIND_ENTRIES` dans `web/tests/fixtures.ts`.

- [ ] **Step 1 : Fixtures et tests (échec attendu)**

Dans `web/tests/fixtures.ts`, après `TEMP_ENTRY` :

```ts
/** Entrées vent (spec vent §4), hors de MANIFEST pour ne pas changer les comptes des autres suites. */
export const WIND_ENTRIES = {
  wind_u: gfs("wind_u", "UGRD_10m", "m/s", { min: -60, max: 60, scale: "linear" }, { min: -31.2, max: 34.8 }),
  wind_v: gfs("wind_v", "VGRD_10m", "m/s", { min: -60, max: 60, scale: "linear" }, { min: -28.9, max: 30.1 }),
};
```

`web/tests/wind-loader.test.ts` :

```ts
import { describe, expect, it, vi } from "vitest";
import { TextureError } from "../src/data/loader";
import { parseManifest } from "../src/data/manifest";
import { WindLoader, type WindLoaderDeps } from "../src/wind/loader";
import { GRID, MANIFEST, WIND_ENTRIES } from "./fixtures";

const BASE = "https://example.test/layers";
const M = parseManifest({ ...MANIFEST, layers: { ...MANIFEST.layers, ...WIND_ENTRIES } });
const U = M.layers.wind_u!;
const V = M.layers.wind_v!;

function fakeBitmap(width = 1440, height = 721): ImageBitmap {
  return { width, height, close: vi.fn() } as unknown as ImageBitmap;
}

function deps(opts: { bitmaps?: Record<string, ImageBitmap>; pixels?: (b: ImageBitmap) => Uint8ClampedArray | null; fail?: RegExp } = {}) {
  const fetchBitmap = vi.fn(async (url: string) => {
    if (opts.fail?.test(url)) throw new Error(`HTTP 500 sur ${url}`);
    return opts.bitmaps?.[url] ?? fakeBitmap();
  });
  return { fetchBitmap, bitmapPixels: opts.pixels ?? ((b) => new Uint8ClampedArray(b.width * b.height * 4)) } satisfies WindLoaderDeps;
}

describe("WindLoader — spec vent §8", () => {
  it("charge U et V ensemble, pixels à la taille de la grille, bitmaps fermés", async () => {
    const d = deps();
    const wl = new WindLoader(BASE, d);
    const f = await wl.load(U, V, GRID);
    expect(f).not.toBeNull();
    expect(d.fetchBitmap.mock.calls.map((c) => c[0])).toEqual([
      `${BASE}/wind_u.png?v=2026-09-12T14%3A12%3A40Z`, `${BASE}/wind_v.png?v=2026-09-12T14%3A12%3A40Z`,
    ]);
    expect(f!.u.length).toBe(1440 * 721 * 4);
    expect(f!.encU).toEqual(U.encoding);
    expect(f!.grid).toEqual({ width: 1440, height: 721 });
    expect(wl.field).toBe(f);
  });
  it("même generated_at : null, aucun fetch ; generated_at neuf : rechargé", async () => {
    const d = deps();
    const wl = new WindLoader(BASE, d);
    await wl.load(U, V, GRID);
    expect(await wl.load(U, V, GRID)).toBeNull();
    expect(d.fetchBitmap).toHaveBeenCalledTimes(2);
    const U2 = { ...U, generated_at: "2026-09-12T15:12:40Z" };
    const V2 = { ...V, generated_at: "2026-09-12T15:12:40Z" };
    const f2 = await wl.load(U2, V2, GRID);
    expect(f2).not.toBeNull();
    expect(d.fetchBitmap).toHaveBeenCalledTimes(4);
  });
  it("V en échec : rejet, bitmap U fermé, l'ancien champ reste", async () => {
    const d = deps();
    const wl = new WindLoader(BASE, d);
    const first = await wl.load(U, V, GRID);
    const bu = fakeBitmap();
    const failing = deps({ fail: /wind_v/, bitmaps: { [`${BASE}/wind_u.png?v=2026-09-12T14%3A12%3A40Z`]: bu } });
    const wl2 = new WindLoader(BASE, failing);
    await expect(wl2.load(U, V, GRID)).rejects.toThrow(/wind_v/);
    expect(bu.close).toHaveBeenCalled();
    expect(wl2.field).toBeNull();
    expect(wl.field).toBe(first);
  });
  it("dimensions ≠ grille : TextureError, bitmaps fermés", async () => {
    const bad = fakeBitmap(1441, 721);
    const good = fakeBitmap();
    const d = deps({ bitmaps: { [`${BASE}/wind_u.png?v=2026-09-12T14%3A12%3A40Z`]: bad, [`${BASE}/wind_v.png?v=2026-09-12T14%3A12%3A40Z`]: good } });
    await expect(new WindLoader(BASE, d).load(U, V, GRID)).rejects.toThrow(TextureError);
    expect(bad.close).toHaveBeenCalled();
    expect(good.close).toHaveBeenCalled();
  });
  it("pixels illisibles (null) : rejet", async () => {
    const d = deps({ pixels: () => null });
    await expect(new WindLoader(BASE, d).load(U, V, GRID)).rejects.toThrow(/pixels/);
  });
  it("non réentrant : deux load concurrents partagent la même promesse", async () => {
    const d = deps();
    const wl = new WindLoader(BASE, d);
    const [a, b] = await Promise.all([wl.load(U, V, GRID), wl.load(U, V, GRID)]);
    expect(a).toBe(b);
    expect(d.fetchBitmap).toHaveBeenCalledTimes(2);
  });
  it("dispose vide le champ", async () => {
    const wl = new WindLoader(BASE, deps());
    await wl.load(U, V, GRID);
    wl.dispose();
    expect(wl.field).toBeNull();
  });
});
```

- [ ] **Step 2 : Vérifier l'échec**

Run: `npm --prefix web run test -- tests/wind-loader.test.ts`
Expected: FAIL — module introuvable.

- [ ] **Step 3 : Implémenter**

`web/src/wind/loader.ts` :

```ts
/**
 * Chargement des deux PNG U/V du vent (spec vent §8) : pixels CPU seulement, jamais de
 * texture GPU. Les deux composantes sont remplacées ensemble. Non réentrant.
 */
import { TextureError, browserDeps, needsTextureFetch, textureUrl } from "../data/loader";
import type { Grid, LayerEntry } from "../data/manifest";
import type { WindField } from "./sim";

export interface WindLoaderDeps {
  fetchBitmap(url: string): Promise<ImageBitmap>;
  bitmapPixels(bitmap: ImageBitmap): Uint8ClampedArray | null;
}

function close(b: ImageBitmap): void {
  if (typeof b.close === "function") b.close();
}

export class WindLoader {
  private current: { field: WindField; entryU: LayerEntry; entryV: LayerEntry } | null = null;
  private inflight: Promise<WindField | null> | null = null;

  constructor(
    private readonly baseUrl: string,
    private readonly deps: WindLoaderDeps = browserDeps,
  ) {}

  get field(): WindField | null {
    return this.current?.field ?? null;
  }

  /** `null` si les deux entrées ont le même `generated_at` que le champ courant. Lève si l'un des PNG échoue. */
  load(entryU: LayerEntry, entryV: LayerEntry, grid: Pick<Grid, "width" | "height">): Promise<WindField | null> {
    if (this.inflight) return this.inflight;
    const run = async (): Promise<WindField | null> => {
      const prev = this.current;
      if (prev && !needsTextureFetch(prev.entryU, entryU) && !needsTextureFetch(prev.entryV, entryV)) return null;
      const results = await Promise.allSettled([
        this.deps.fetchBitmap(textureUrl(this.baseUrl, entryU)),
        this.deps.fetchBitmap(textureUrl(this.baseUrl, entryV)),
      ]);
      const got = results.filter((r): r is PromiseFulfilledResult<ImageBitmap> => r.status === "fulfilled").map((r) => r.value);
      const failed = results.find((r): r is PromiseRejectedResult => r.status === "rejected");
      if (failed) {
        for (const b of got) close(b); // l'autre composante a peut-être réussi : ne pas la fuir
        throw failed.reason instanceof Error ? failed.reason : new Error(String(failed.reason));
      }
      const [bu, bv] = got as [ImageBitmap, ImageBitmap];
      try {
        for (const b of [bu, bv]) {
          if (b.width !== grid.width || b.height !== grid.height) {
            throw new TextureError(`vent ${b.width}×${b.height}, grille ${grid.width}×${grid.height} attendue`);
          }
        }
        const u = this.deps.bitmapPixels(bu);
        const v = this.deps.bitmapPixels(bv);
        if (!u || !v) throw new Error("pixels du vent illisibles");
        const field: WindField = { u, v, grid: { width: grid.width, height: grid.height }, encU: entryU.encoding, encV: entryV.encoding };
        this.current = { field, entryU, entryV };
        return field;
      } finally {
        close(bu);
        close(bv);
      }
    };
    this.inflight = run().finally(() => {
      this.inflight = null;
    });
    return this.inflight;
  }

  dispose(): void {
    this.current = null;
  }
}
```

- [ ] **Step 4 : Vérifier**

Run: `npm --prefix web run test && npm --prefix web run typecheck`
Expected: PASS (toutes les suites ; `fixtures.ts` modifié ne change aucun compte existant).

- [ ] **Step 5 : Commit**

```bash
git add web/src/wind/loader.ts web/tests/wind-loader.test.ts web/tests/fixtures.ts
git commit -m "feat(web): WindLoader — pixels CPU des deux PNG U/V, remplacement atomique"
```

---

### Task 9 : `SceneHandle.onFrame` et `WindController`

**Files:**
- Modify: `web/src/render/scene.ts`
- Create: `web/src/wind/controller.ts`
- Test: `web/tests/wind-controller.test.ts` (nouveau)

**Interfaces:**
- Consumes: `WindSim`, `WindField`, `PickFn`, `TICK_MS`, `MAX_DT_S` de `web/src/wind/sim.ts` ; `WindLayer` de `web/src/render/wind.ts` ; `viewStateFrom`, `mapStyleFor` de `web/src/tiles/lod.ts`.
- Produces:
  - `SceneHandle.onFrame(cb: (nowMs: number) => boolean): () => void` (désinscription) — la boucle rend si un callback renvoie `true` ;
  - `interface WindControllerDeps { sim: Pick<WindSim, "step">; layer: Pick<WindLayer, "markDirty" | "setMapStyle">; camera: THREE.PerspectiveCamera; viewportHeight(): number; pick: PickFn }` ;
  - `class WindController { constructor(deps: WindControllerDeps); setField(f: WindField | null): void; get field(): WindField | null; frame(nowMs: number): boolean }`.

- [ ] **Step 1 : Tests (échec attendu)**

`web/tests/wind-controller.test.ts` :

```ts
import * as THREE from "three";
import { describe, expect, it, vi } from "vitest";
import { WindController } from "../src/wind/controller";
import { MAX_DT_S, TICK_MS, type WindField } from "../src/wind/sim";

const FIELD = { u: new Uint8ClampedArray(4), v: new Uint8ClampedArray(4), grid: { width: 1, height: 1 }, encU: { bits: 8, min: -60, max: 60, scale: "linear" as const }, encV: { bits: 8, min: -60, max: 60, scale: "linear" as const } } satisfies WindField;

function make() {
  const camera = new THREE.PerspectiveCamera(45, 1, 0.01, 10);
  camera.position.set(0, 0, 1.1);
  camera.lookAt(0, 0, 0);
  const sim = { step: vi.fn() };
  const layer = { markDirty: vi.fn(), setMapStyle: vi.fn() };
  const ctl = new WindController({ sim, layer, camera, viewportHeight: () => 800, pick: () => null });
  return { ctl, sim, layer };
}

describe("WindController — spec vent §8", () => {
  it("sans champ : jamais de tick, false", () => {
    const { ctl, sim } = make();
    expect(ctl.frame(0)).toBe(false);
    expect(ctl.frame(100)).toBe(false);
    expect(sim.step).not.toHaveBeenCalled();
  });
  it("premier frame après setField amorce l'horloge sans tick ; tick une fois 33 ms écoulées", () => {
    const { ctl, sim, layer } = make();
    ctl.setField(FIELD);
    expect(ctl.frame(1000)).toBe(false);
    expect(ctl.frame(1020)).toBe(false);
    expect(ctl.frame(1040)).toBe(true);
    expect(sim.step).toHaveBeenCalledTimes(1);
    expect(sim.step.mock.calls[0]![1]).toBeCloseTo(0.04, 9);
    expect(layer.markDirty).toHaveBeenCalledTimes(1);
    expect(layer.setMapStyle).toHaveBeenCalledWith(1); // d = 1,1 < MAP_FADE_END (1,14) → style carte
  });
  it("un seul tick par frame, dt borné à MAX_DT_S", () => {
    const { ctl, sim } = make();
    ctl.setField(FIELD);
    ctl.frame(0);
    expect(ctl.frame(5000)).toBe(true);
    expect(sim.step).toHaveBeenCalledTimes(1);
    expect(sim.step.mock.calls[0]![1]).toBeCloseTo(MAX_DT_S, 9);
    expect(ctl.frame(5000 + TICK_MS / 2)).toBe(false);
  });
  it("setField(null) arrête et réamorce l'horloge au prochain champ", () => {
    const { ctl, sim } = make();
    ctl.setField(FIELD);
    ctl.frame(0);
    ctl.setField(null);
    expect(ctl.frame(1000)).toBe(false);
    ctl.setField(FIELD);
    expect(ctl.frame(2000)).toBe(false); // réamorçage : pas de tick de 1 s
    expect(ctl.frame(2040)).toBe(true);
    expect(sim.step).toHaveBeenCalledTimes(1);
    expect(sim.step.mock.calls[0]![1]).toBeCloseTo(0.04, 9);
  });
  it("la vue passée au step vient de la caméra courante", () => {
    const { ctl, sim } = make();
    ctl.setField(FIELD);
    ctl.frame(0);
    ctl.frame(40);
    const view = sim.step.mock.calls[0]![2];
    expect(view.cameraPosition.length()).toBeCloseTo(1.1, 9);
    expect(view.viewportHeight).toBe(800);
  });
});
```

- [ ] **Step 2 : Vérifier l'échec**

Run: `npm --prefix web run test -- tests/wind-controller.test.ts`
Expected: FAIL — module introuvable.

- [ ] **Step 3 : `onFrame` dans `scene.ts`**

Dans l'interface `SceneHandle`, après `onViewChange` :

```ts
  /** Callback appelé à chaque frame rAF avec l'horodatage ; renvoyer `true` demande un rendu. Renvoie la désinscription. */
  onFrame(cb: (nowMs: number) => boolean): () => void;
```

Dans `createScene`, après `const viewListeners`, ajouter `const frameListeners = new Set<(nowMs: number) => boolean>();`. La boucle devient :

```ts
  const loop = (now: number) => {
    try {
      let animated = false;
      for (const cb of frameListeners) if (cb(now)) animated = true;
      const zoomed = zoom.beforeUpdate();
      const moved = controls.update() || zoomed;
      if (moved || dirty || animated) {
        dirty = false;
        applyDistance();
        camera.updateMatrixWorld(true);
        // hauteur CSS, pas framebuffer : sur écran 2× on reste un niveau plus grossier, choix de budget (spec §5).
        const view = viewStateFrom(camera, canvas.clientHeight);
        for (const cb of viewListeners) cb(view);
        renderer.render(scene, camera);
      }
    } catch (e) {
      // Un listener de vue qui lève (ex. sélection de tuiles) ne doit jamais arrêter la boucle de rendu.
      console.error(e);
    } finally {
      requestAnimationFrame(loop);
    }
  };
```

et dans l'objet renvoyé :

```ts
    onFrame(cb) {
      frameListeners.add(cb);
      return () => {
        frameListeners.delete(cb);
      };
    },
```

`start()` appelle déjà `requestAnimationFrame(loop)` : rAF fournit `now`.

- [ ] **Step 4 : Implémenter `wind/controller.ts`**

```ts
/**
 * Cadence de la simulation (spec vent §8) : un tick par frame au plus, quand ≥ TICK_MS se sont
 * écoulées, dt borné à MAX_DT_S. Inscrit sur SceneHandle.onFrame seulement quand le vent est actif.
 */
import type * as THREE from "three";
import type { WindLayer } from "../render/wind";
import { mapStyleFor, viewStateFrom } from "../tiles/lod";
import { MAX_DT_S, TICK_MS, type PickFn, type WindField, type WindSim } from "./sim";

export interface WindControllerDeps {
  sim: Pick<WindSim, "step">;
  layer: Pick<WindLayer, "markDirty" | "setMapStyle">;
  camera: THREE.PerspectiveCamera;
  viewportHeight(): number;
  pick: PickFn;
}

export class WindController {
  private current: WindField | null = null;
  private last: number | null = null;
  private acc = 0;

  constructor(private readonly deps: WindControllerDeps) {}

  get field(): WindField | null {
    return this.current;
  }

  setField(f: WindField | null): void {
    this.current = f;
    this.last = null;
    this.acc = 0;
  }

  /** Callback de `SceneHandle.onFrame` : `true` si un tick a eu lieu (rendu nécessaire). */
  frame(nowMs: number): boolean {
    if (!this.current) return false;
    if (this.last === null) {
      this.last = nowMs;
      return false;
    }
    this.acc += nowMs - this.last;
    this.last = nowMs;
    if (this.acc < TICK_MS) return false;
    const dt = Math.min(this.acc / 1000, MAX_DT_S);
    this.acc = 0;
    const { camera, sim, layer } = this.deps;
    camera.updateMatrixWorld(true);
    const view = viewStateFrom(camera, this.deps.viewportHeight());
    sim.step(this.current, dt, view, this.deps.pick);
    layer.setMapStyle(mapStyleFor(view.cameraPosition.length()));
    layer.markDirty();
    return true;
  }
}
```

- [ ] **Step 5 : Vérifier**

Run: `npm --prefix web run test && npm --prefix web run typecheck`
Expected: PASS.

- [ ] **Step 6 : Commit**

```bash
git add web/src/render/scene.ts web/src/wind/controller.ts web/tests/wind-controller.test.ts
git commit -m "feat(web): SceneHandle.onFrame et WindController cadencé à 30 Hz"
```

---

### Task 10 : Interrupteur « Vent », conteneur du menu, tooltip à deux lignes

**Files:**
- Modify: `web/index.html`, `web/src/style.css`, `web/src/ui/overlay.ts`, `web/src/ui/tooltip.ts`, `web/src/main.ts` (une ligne : `ui.controls` → `ui.layersMenu`)
- Create: `web/src/ui/wind-toggle.ts`

**Interfaces:**
- Consumes: `formatWind` (T4), `WindField` (T6), `sampleValue`.
- Produces:
  - `Overlay.layersMenu: HTMLElement` (remplace `controls`), `Overlay.windToggle: HTMLButtonElement` ;
  - `interface WindToggle { setOn(on: boolean): void; setDisabled(disabled: boolean): void }` ; `createWindToggle(button: HTMLButtonElement, onChange: (on: boolean) => void): WindToggle` ;
  - `Tooltip.setWind(field: WindField | null): void` ; le tooltip s'affiche si la couche **ou** le vent a une valeur.

Aucun test Vitest (DOM) : validation à l'œil en T11. Le typecheck garde la cohérence.

- [ ] **Step 1 : DOM et CSS**

`web/index.html`, remplacer `<aside id="controls" class="panel"></aside>` par :

```html
      <aside id="controls" class="panel">
        <div id="layers-menu"></div>
        <button id="wind-toggle" type="button" role="switch" aria-checked="false" disabled>Vent</button>
      </aside>
```

`web/src/style.css` : le sélecteur `#controls [role="radio"]` (règles de base, `[aria-checked="true"]`, `:disabled`, `:focus-visible`, et la règle mobile `scroll-snap-align`) devient `#controls [role="radio"], #controls [role="switch"]` sur chacune de ces lignes ; ajouter après le bloc `#controls { … }` :

```css
#layers-menu { display: contents; } /* les radios restent des enfants flex directs de #controls, en colonne comme en ligne (mobile) */
#wind-toggle { margin-top: 0.5rem; }
#wind-toggle::before { content: "◌ "; }
#wind-toggle[aria-checked="true"]::before { content: "≋ "; }
```

et dans la règle `#tooltip { … }` ajouter `white-space: pre-line;`. Dans la règle mobile `@media (max-width: 600px)`, ajouter `#wind-toggle { margin-top: 0; }`.

- [ ] **Step 2 : `overlay.ts`**

Dans `Overlay` remplacer `controls: HTMLElement;` par :

```ts
  /** Conteneur du menu radio des couches (ui/layers-menu.ts). */
  layersMenu: HTMLElement;
  /** Bouton role="switch" du vent (ui/wind-toggle.ts). */
  windToggle: HTMLButtonElement;
```

Dans `createOverlay` : `const layersMenu = byId<HTMLElement>("layers-menu"); const windToggle = byId<HTMLButtonElement>("wind-toggle");` (retirer `controls`), et retourner `layersMenu, windToggle,` à la place de `controls,`. Dans `web/src/main.ts`, `createLayersMenu(ui.controls, …)` devient `createLayersMenu(ui.layersMenu, …)`.

- [ ] **Step 3 : `ui/wind-toggle.ts`**

```ts
/** Interrupteur « Vent » (spec vent §9) : DOM seulement ; l'état initial et l'URL viennent de wind/select.ts. */
export interface WindToggle {
  setOn(on: boolean): void;
  setDisabled(disabled: boolean): void;
}

export function createWindToggle(button: HTMLButtonElement, onChange: (on: boolean) => void): WindToggle {
  let on = false;
  const render = () => button.setAttribute("aria-checked", String(on));
  button.addEventListener("click", () => {
    if (button.disabled) return;
    on = !on;
    render();
    onChange(on);
  });
  render();
  return {
    setOn(v) {
      on = v;
      render();
    },
    setDisabled(disabled) {
      button.disabled = disabled;
      button.title = disabled ? "Vent indisponible" : "";
    },
  };
}
```

- [ ] **Step 4 : `tooltip.ts` — deuxième ligne**

Imports : ajouter `import type { WindField } from "../wind/sim";` et `formatWind` à l'import de `./format`. Dans l'interface `Tooltip`, après `setData` :

```ts
  /** Champ de vent actif (spec vent §10) ; `null` retire la ligne « Vent … ». */
  setWind(field: WindField | null): void;
```

Dans `createTooltip` : `let wind: WindField | null = null;` ; `refreshText` devient :

```ts
  const refreshText = () => {
    if (!reading || (!data && !wind)) return;
    const lines: string[] = [];
    if (data) lines.push(formatReading(data.def, sampleValue(data.pixels, data.grid, data.encoding, reading.lon, reading.lat)));
    if (wind) {
      const u = sampleValue(wind.u, wind.grid, wind.encU, reading.lon, reading.lat);
      const v = sampleValue(wind.v, wind.grid, wind.encV, reading.lon, reading.lat);
      lines.push(formatWind(u, v));
    }
    tip.textContent = lines.join("\n");
  };
```

Ajouter la méthode :

```ts
    setWind(f) {
      wind = f && f.u.length === f.grid.width * f.grid.height * 4 && f.v.length === f.u.length ? f : null;
      refreshText();
    },
```

et dans `update(camera, width, height)` remplacer la garde `if (!reading || !data)` par `if (!reading || (!data && !wind))`.

- [ ] **Step 5 : Vérifier**

Run: `npm --prefix web run typecheck && npm --prefix web run test && npm --prefix web run build`
Expected: PASS ; build OK. Lancer `npm --prefix web run dev`, ouvrir `http://localhost:5173/` : le bouton « Vent » apparaît grisé sous le radio (données de prod sans `wind_*`), disposition desktop et ≤ 600 px intacte (radios en ligne, switch à la suite).

- [ ] **Step 6 : Commit**

```bash
git add web/index.html web/src/style.css web/src/ui/overlay.ts web/src/ui/wind-toggle.ts web/src/ui/tooltip.ts web/src/main.ts
git commit -m "feat(web): interrupteur Vent, conteneur #layers-menu, tooltip à deux lignes"
```

---

### Task 11 : Câblage `main.ts` et validation navigateur

**Files:**
- Modify: `web/src/main.ts`, `.gitignore`
- Test: validation manuelle DevTools MCP (critères 1 à 6 et 8 de la spec §13), rapport `.superpowers/sdd/2026-09-13-wind/validation-report.md`

**Interfaces:**
- Consumes: tout ce qui précède : `WIND_PROFILE`, `WindSim`, `createWindLayer`, `WindLoader`, `WindController`, `parseWindParam`, `withWindParam`, `createWindToggle`, `tooltip.setWind`, `sceneHandle.onFrame`, `pickSphere`.
- Produces: site fonctionnel avec vent ; `windNotice` dans `refreshBanner`.

- [ ] **Step 1 : Imports et objets**

Dans `web/src/main.ts`, imports supplémentaires :

```ts
import { createWindLayer } from "./render/wind";
import { WindController } from "./wind/controller";
import { WindLoader } from "./wind/loader";
import { parseWindParam, withWindParam } from "./wind/select";
import { WIND_PROFILE, WindSim } from "./wind/sim";
import { createWindToggle } from "./ui/wind-toggle";
```

Après `sceneHandle.scene.add(globe.group);` :

```ts
  const windProfile = WIND_PROFILE[decision.tier];
  const windSim = new WindSim(windProfile.particles, windProfile.trail);
  const windLayer = createWindLayer(windSim.positions, windProfile.particles, windProfile.trail);
  sceneHandle.scene.add(windLayer.object);
  const windCtl = new WindController({
    sim: windSim,
    layer: windLayer,
    camera: sceneHandle.camera,
    viewportHeight: () => canvas.clientHeight,
    pick: (x, y, target) => pickSphere(x, y, sceneHandle.camera, target),
  });
```

Après `const luts = …` et les autres `let` d'état :

```ts
  const windLoader = new WindLoader(DATA_BASE_URL);
  let windOn = parseWindParam(location.search, decision.tier, matchMedia("(prefers-reduced-motion: reduce)").matches);
  let windUnsub: (() => void) | null = null;
  /** generated_at de l'entrée wind_u qui a échoué ; retentable quand il change (spec vent §11). */
  let windFailedAt: string | null = null;
  /** Statut « Vent indisponible », après layerNotice dans l'ordre de priorité. */
  let windNotice: string | null = null;
  const windToggle = createWindToggle(ui.windToggle, (on) => {
    windOn = on;
    history.replaceState(null, "", withWindParam(location.search, on));
    void applyWind();
  });
  windToggle.setOn(windOn);
  // Crochet de validation (T11, critères 2, 3, 8) : dev seulement, comme `__worldtemp`.
  if (import.meta.env.DEV) {
    (window as unknown as { __worldtempWind: unknown }).__worldtempWind = { sim: windSim, controller: windCtl, loader: windLoader };
  }
```

- [ ] **Step 2 : `refreshBanner`**

Dans les deux appels `ui.setStatus(…)` de `refreshBanner`, remplacer `layerNotice ??` par `layerNotice ?? windNotice ??`.

- [ ] **Step 3 : `applyWind` / `stopWind`**

Avant `applyData` :

```ts
  const stopWind = () => {
    windUnsub?.();
    windUnsub = null;
    windCtl.setField(null);
    tooltip.setWind(null);
    windLayer.object.visible = false;
    sceneHandle.requestRender();
  };

  /** Active/désactive le vent selon `windOn` et le manifeste courant ; charge U/V si besoin (spec vent §8, §11). */
  const applyWind = async (): Promise<void> => {
    const manifest = manifests.manifest;
    const entryU = manifest?.layers["wind_u"];
    const entryV = manifest?.layers["wind_v"];
    const available = !!manifest && !!entryU && !!entryV && windFailedAt !== entryU.generated_at;
    windToggle.setDisabled(!available);
    if (!windOn || !available) {
      stopWind();
      return;
    }
    try {
      await windLoader.load(entryU!, entryV!, manifest!.grid);
      windNotice = null;
    } catch (e) {
      console.warn("[worldtemp] vent indisponible :", e);
      windFailedAt = entryU!.generated_at;
      if (!windLoader.field) {
        windToggle.setDisabled(true);
        windNotice = "Vent indisponible";
        refreshBanner();
        stopWind();
        return;
      }
      // ancien champ encore là : on continue de l'animer, retentable au prochain generated_at
    }
    if (!windOn) return; // basculé pendant le chargement
    windCtl.setField(windLoader.field);
    tooltip.setWind(windLoader.field);
    windLayer.object.visible = true;
    if (!windUnsub) windUnsub = sceneHandle.onFrame((t) => windCtl.frame(t));
    refreshBanner();
  };
```

Dans `applyData`, juste avant le `refreshBanner();` qui suit le bloc `if (fresh) { … } else if (…) { … }`, ajouter `await applyWind();`. Dans le `catch` de `applyData`, ajouter `await applyWind();` avant `refreshBanner();` (manifeste absent → switch désactivé). Dans le gestionnaire `webglcontextlost`, ajouter `stopWind();` avant `ui.showFatal(…)`.

- [ ] **Step 4 : Typecheck, tests, build, budget**

Run: `npm --prefix web run typecheck && npm --prefix web run test && npm --prefix web run build`
Expected: PASS ; noter la taille gzip de `dist/assets/index-*.js` (sortie de `vite build`) : **≤ 162,70 Ko**.

- [ ] **Step 5 : Données locales avec le vent**

La prod n'a pas encore `wind_*`. Récupérer le dry-run de la CI de cette branche (artefact `dry-run`, job `test`, T3 ou dernier push) :

```bash
gh run list --branch feat/wind --workflow test.yml --limit 1
gh run download <run-id> -n dry-run -D web/public/dev-data
ls web/public/dev-data/layers   # latest.json + 9 PNG dont wind_u.png, wind_v.png
```

Ajouter `web/public/dev-data/` à `.gitignore` (commité dans cette tâche). Servir : `VITE_DATA_BASE_URL=/dev-data/layers npm --prefix web run dev` (PowerShell : `$env:VITE_DATA_BASE_URL='/dev-data/layers'; npm --prefix web run dev`). Ouvrir `http://localhost:5173/?wind=1`.

- [ ] **Step 6 : Validation navigateur (DevTools MCP)**

Charger `mcp__claude-in-chrome__*` (ou `mcp__brave-devtools__*` si l'extension Chrome est injoignable, comme le 2026-09-12). Mesurer et consigner dans `.superpowers/sdd/2026-09-13-wind/validation-report.md` :

1. **Critère 1** : `?wind=1&layer=temp` puis `?wind=1&layer=none` : traînées blanches sur satellite (d = 3), sombres après zoom sous d = 1,14 ; capture d'écran ; aucune traînée sur le limbe ni au travers du globe (regarder le bord du disque).
2. **Critère 2** : dans la console, `const s = __worldtempWind.sim, c = __worldtemp.camera` ; relever la position de tête d'une particule visible (`i` tel que `s.age[i] > 0`) : `p0 = [s.positions.slice((s.trail-1)*s.count*3 + i*3, …+3)]`, projeter avec `new THREE.Vector3(...p0).project(c)` (Three est accessible via `import("three")` dans la console Vite) → px ; recommencer 1 s plus tard (`age` a avancé de 30) ; lire la vitesse V km/h au tooltip du même point ; attendu déplacement ≈ 2·V/3,6 px/s ± 25 %, à d = 4 puis d = 1,1.
3. **Critère 3** : survoler un point, lire « Vent N km/h DIR » ; recalculer avec un script Python sur `web/public/dev-data/layers/wind_u.png`/`wind_v.png` (Pillow → tableau, colonne `round((lon+180)/0,25)`, ligne `round((90−lat)/0,25)`, `pipeline.texture.dequantize` avec `Encoding(-60, 60, "linear")`, puis `3,6·hypot`, direction `(270 − atan2(v, u)) mod 360`) : ±2 km/h, ±1 point de rose.
4. **Critère 4** : `?wind=0` : compter les rendus sur 3 s au repos (interception `renderer.render` via le crochet dev ou `performance.getEntries`) = 0 ; `?wind=1` : ≤ 30 rendus/s (DevTools Performance, ou compteur sur 3 s ≤ 92).
5. **Critère 5** : `?wind=0` sur desktop → switch décoché, aucune requête `wind_*.png` (onglet réseau) ; `?tier=low` sans `wind` → décoché ; clic sur le switch → `location.search` contient `wind=1`, pas de rechargement (le `performance.timeOrigin` ne change pas), requêtes `wind_u.png` et `wind_v.png` parties.
6. **Critère 6** : éditer `web/public/dev-data/layers/latest.json` pour retirer `wind_u` et `wind_v`, recharger avec `?wind=1` : switch grisé, 7 couches intactes, aucune requête `wind_*.png`. Remettre le fichier.
7. **Critère 8 (tick)** : dans la console, `const c = __worldtempWind.controller; const f = c.frame.bind(c); let n = 0, tot = 0; c.frame = (t) => { const a = performance.now(); const r = f(t); if (r) { n++; tot += performance.now() - a; } return r; };` puis après 3 s `tot / n` : moyenne ≤ 4 ms en `high`. (Le callback inscrit par `onFrame` appelle `windCtl.frame(t)` par une fermeture : le remplacement de la méthode est pris en compte.)
8. **Mobile** : `?tier=low` à 500 px de large (plancher outil) : switch après les radios en ligne, vent désactivé par défaut, activable, fluide.

Console propre (aucune erreur). Tout écart → corriger dans cette tâche, re-mesurer, consigner.

- [ ] **Step 7 : Commit**

```bash
git add web/src/main.ts .gitignore .superpowers/sdd/2026-09-13-wind/validation-report.md
git commit -m "feat(web): câblage du vent — applyWind, statut « Vent indisponible », validation navigateur"
git push
```

Vérifier la CI (`gh run watch`) : `test` et `web` verts.

---

### Task 12 : HISTORY.md

**Files:**
- Modify: `HISTORY.md` (§3, §5, §7, §8, §9, pied de page)

**Interfaces:** aucune ; `tools/history_check.py` doit passer (il exige que §3 nomme tout dossier suivi par git, dont `web/src/wind/`, et que le pied de page soit à jour).

- [ ] **Step 1 : Mettre à jour**

Invoquer le skill `updating-history` (dérive les sections depuis `git diff master...feat/wind --stat`). Points obligatoires :

- **§3** : `pipeline/config.py`, `metadata.py`, `main.py` (legacy retiré), `layers.py` (9 specs), `tests/fixtures/gfs_wind.grib2` ; `web/src/wind/` (`sim.ts`, `select.ts`, `loader.ts`, `controller.ts`), `web/src/render/wind.ts` + `shaders/wind.*.glsl`, `scene.ts` (`onFrame`), `ui/wind-toggle.ts`, `ui/tooltip.ts` (`setWind`), `ui/format.ts` (`formatWind`), `overlay.ts` (`layersMenu`, `windToggle`), `index.html` (`#layers-menu`, `#wind-toggle`) ; `web/tests/wind-*.test.ts`, `fixtures.ts` (`WIND_ENTRIES`).
- **§5** : ligne « gfs/latest.* republié une version » barrée/remplacée ; nouvelles décisions : surimpression combinable, simulation CPU + `LineSegments` plutôt que GPU ping-pong (pourquoi : testable, pas de render target flottant, traînées stables en rotation), deux entrées scalaires plutôt qu'un PNG RGB, upload complet du tampon plutôt qu'`updateRange`, vitesse apparente constante, spawn en NDC, actif par défaut `high` seulement.
- **§7** : ligne pour la spec vent (2026-09-13, plan 12 tâches).
- **§8** : dette n° 31 ✅ résolue (date, commit) ; nouvelles dettes éventuelles (largeur 1 px, couleur par vitesse, mesures reportées).
- **§9** : entrée `2026-09-13 — Spec 4 lot B2 (vent) exécutée sur feat/wind` : livré, tests (compte pytest/vitest réel), build (gzip mesuré), critères mesurés, revue, prochaine action (merge, `gh workflow run pipeline.yml`, suppression manuelle de `gfs/latest.*` sur R2, puis lot C).
- Pied de page : date et résumé.

- [ ] **Step 2 : Vérifier**

Run: `.venv/Scripts/python tools/history_check.py && .venv/Scripts/python -m pytest tests/test_history_check.py -q`
Expected: `✓ HISTORY.md est à jour`, tests verts.

- [ ] **Step 3 : Commit et push**

```bash
git add HISTORY.md
git commit -m "docs(history): spec vent lot B2 exécutée — 9 couches, particules CPU, legacy retiré"
git push
```

Puis `superpowers:finishing-a-development-branch` : revue finale de branche, merge `--no-ff` dans `master`, push, CI `deploy`, `gh workflow run pipeline.yml`, contrôle `https://data.globelayers.com/layers/latest.json` (9 entrées) et `https://globelayers.com/?wind=1`, suppression de `gfs/latest.png` et `gfs/latest.json` sur R2 (API Cloudflare), mise à jour finale de HISTORY §9.
